REFLECTION

1. I faced several challenges including for instance applying rounded corner to the card and the image. I had to customize the card to apply rounded corner in the css file.  
2. Bootstrap utility classes and components greatly simply the styling process by offering ready to use classes such as for padding, margin, etc. which significantly reduced the amount of code to write in the css file. In addition, there are several components available for most use cases. 
3. Scenarios I might choose not to use Bootstrap include when the styling requires unique design not being offer in Bootstrap library or for a very simple project. 